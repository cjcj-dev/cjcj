package cangjie_compiler::modules

public class ExprWriter {
    public init() {}

    public func WriteExprTag(tag: String): String {
        // TODO(selfhost:Modules): write real AST expressions into flatbuffer expression tables.
        return tag
    }
}
