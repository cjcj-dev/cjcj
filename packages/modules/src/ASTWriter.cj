package cangjie_compiler::modules

// ASTWriter is implemented in ASTSerialization.cj so the public loader/writer API remains co-located.
// TODO(selfhost:Modules): split writer internals here once real flatbuffer AST writing replaces the local wire writer.
