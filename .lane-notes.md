# cjcj#56 WarmupCache

C++: SearchSymbol.cpp:33-75 WarmupCache spawns std::thread per scope; Ptr is raw Symbol*.
cjcj: TypeCheckerImpl.cj:223 WarmupCache used TaskQueue; Ptr.lt uses Identifiable.objectId → TypeInfo::GetMTable.

On 192-core kkk2, concurrent generic TI / GetMTable in host runtime SIGSEGV during Searcher.Union.
Control: taskset 0-31 N=4 rc=0; unrestricted N=3 2×139 1×0.

Fix: same searchers + queries, sequential Search (no TaskQueue). Skip conditions unchanged (C++:36-44).
