# cjcj#206 notes

上游锚（cangjie_compiler HEAD 367d9ca8845ba8ef5fd9c17ea983c9692e303f62，非 ZGC）：

- InsertStringConversions.cpp:38-49 `IsNSString` 后 `GetMemberDecl<FUNC_DECL>(IsGeneratedNSStringCtor)`，`CJC_NULLPTR_CHECK`。
- InsertStringConversions.cpp:52-59 `IsNSObject` 后 `GetMemberDecl<FUNC_DECL>(IsGeneratedNSObjectToString)`。
- ASTQuery.cpp:276-283 `IsNSString`/`IsNSObject` 走 `NameGenerator::GetObjCDeclName`。
- ASTQuery.cpp:628-636 `IsGeneratedNSStringCtor` 用 `type->symbol->name == STD_LIB_STRING`。谓词不改。

我方：

- AfterTypeCheck.cj InsertStringConversions.HandleImpl：去掉对 `Option<FuncDecl>` 的 `as FuncDecl`，改为 `GetFuncMember(...).getOrThrow()`。
- 分路改为已有 `IsNSString` / `IsNSObject`（ASTQuery.cj）。

# lane notes · cjcj#174

上游锚（cangjie_compiler，含 e9abf014+50ec84e5，不含 e7b33cfd 的 swap）：

- ParseMacro.cpp:48 ParseForeignNodes
- ParseMacro.cpp:76 展开入口把 FOREIGN 修饰符赋给 foreignBlockModifiers（赋值，不 swap）
- ParseMacro.cpp:119 TOPLEVEL && Seeing(FOREIGN) → ParseForeignNodes + continue
- ParseMacro.cpp:125 后续声明 ParseDecl(scope, foreignBlockModifiers)
- ParseMacro.cpp:617 继承修饰符写入 macroCall，只对显式修饰符诊断
- Parser.cpp:346 ParseForeignDecls 赋值 foreignBlockModifiers，:367 clear
- ParseModifiers.cpp:33 SetDeclBeginPos 用 !empty 代替 inForeignBlock
- ParserImpl.cpp:23 Parser::ParseDecl 传入 foreignBlockModifiers
- ParserImpl.cpp:26 SetForeignBlockModifiers 赋值
- Parser.h:129 SetForeignBlockModifiers
- MacroEvaluation.cpp:370 / :671 SetForeignBlockModifiers(macCall.GetModifiers())
- ParserImpl.h:146 数据从 bool inForeignBlock 改为修饰符集合

我方落点：

- packages/parse/src/ParserImpl.cj foreignBlockModifiers
- packages/parse/src/Parser.cj ParseDecl / SetForeignBlockModifiers
- packages/parse/src/ParseDecl.cj ParseForeignDecls / SetDeclBeginPos
- packages/parse/src/ParserUtils.cj ApplyModifiersAndAnnotations（仓颉把 begin 调整拆到这里，同一条件）
- packages/parse/src/ParseMacro.cj ParseForeignNodes / ParseNodesImpl / ParseMacroCall
- packages/macro/src/MacroEvaluation.cj 两处 SetForeignBlockModifiers

不移植 e7b33cfd：ArrayList 可赋值。引用语义下赋值会共享，按 std::set 值拷贝复制元素，不用 swap。

# cjcj#185 rework

上游锚（cangjie_compiler 367d9ca8，非 ZGC）：

- AttributePack.cpp:114 JAVA_CJ_MAPPING(deprecated)
- AttributePack.cpp:119 OBJ_C_CJ_MAPPING(deprecated)
- AttributePack.cpp:124 CJ_MIRROR_JAVA_INTERFACE_FWD(deprecated)
- AttributePack.cpp:132 CJ_MIRROR_JAVA_INTERFACE_DEFAULT(deprecated)
- AttributePack.cpp:137 CJ_MIRROR_OBJC_INTERFACE_FWD(deprecated)
- 枚举项与序数不动。注释不另抄；本仓先例是 MACRO_EXPANDED_NODE(deprecated) 纯显示名。

我方：

- packages/ast/src/AttributePack.cj ATTRIBUTE_NAMES 五个显示名加 (deprecated)
- test/does_not_throw/README.md 不再指向已删选项，改为 finalizer 已随 1e503327 删除

# cjcj#345

上游锚（cangjie_compiler 71b92a0b，非 ZGC）：

- Option.cpp:545-583 CheckLtoOptions。Darwin/Windows 仍拒绝；iOS 仍要 --experimental；混淆仍拒绝。8afb7475 删掉的 staticlib-only Errorln 不在终态里。obj 拒绝仍在其后。
- Option.cpp:627-654 CheckLTOPkgVisibilityOptions。未开 LTO 仍 return false。isValid 在 Option.cpp:644-646：IOS，或 LINUX 且 SHARED_LIB。无效只 DiagnoseRefactor(driver_ineffective_option, "--lto-keep-pkg-visibility")，不 return false。
- DiagnosticDriver.def:77 WARNING(driver_ineffective_option, DRIVER_ARG, "%s does not take effect under the current configuration")。

我方落点：

- packages/option/src/Option.cj PerformPostActions 的 LTO 段，删 iOS staticlib-only 检查。
- 同一函数里可见性段改为 Option.cpp:644-649 的 isValid，警告不 return false。
- packages/basic/src/DiagnosticTables.cj 原 driver_visible_pkgs_only_for_dylib（index 97，已是 DS_WARNING + DRIVER_ARG）改名为 driver_ineffective_option，文案改为带 %s 的无效配置警告。
- Darwin/Windows LTO 拒绝、链接参数不改。

