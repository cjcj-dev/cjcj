# cjcj#56 WarmupCache

C++ SearchSymbol.cpp:33-75: per-scope std::thread, per-thread unique_ptr<Searcher>, join, cache.merge.
C++ SafePointer.h:317-319: operator< compares stored T* bits, no vtable.

cjcj: restore TaskQueue (same fork/join as C++ threads). Ptr stores identity Int64 at init; compare/hash use id only (C++ ptr bits), not virtual objectId/GetMTable on TreeSet path.
