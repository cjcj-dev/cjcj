# lane notes · sym_cjcj_343_implement_r5840014143

规格锚是上游 cangjie_compiler，不是 ZGC。按常备裁决（0925 23:5x）编译器包不写虚构 ZGC 锚。runtime 两构型 / 四臂 DIFF / OHOS 不适用。

| 上游 | 不变量 | 我方落点 | 修法 |
|---|---|---|---|
| src/Utils/FileUtil.cpp:495-499 `!= 0` | 掩码与 0 比，SYSTEM/DEVICE/VIRTUAL 可达 | packages/utils/src/FileUtil.cj Windows `canRemovePath` | `== 1u32` 改为 `!= 0u32`。不改非 Windows `canRemovePath` |
| src/Utils/StdUtils/StdUtils.cpp:19 | 跳过 ` \t\n\v\f\r` 后首字符是 `-` 则空；尾部垃圾仍忽略 | packages/utils/src/StdUtils.cj `Stoui`，在 `strtoul` 之前 | `stouiHasLeadingMinus`。不改 `Stol` |
| src/Utils/UserBase.cpp:48-50 `find_first_of(DIR_SEPARATOR)` 循环，每个分隔符改 `-` | 不是只换第一个 `/` | packages/utils/src/UserBase.cj `WriteJson` | 同循环。String 值类型不能 `name[pos]='-'`，重建子串。DIR_SEPARATOR 已有 |

producer → consumer：
- GetFileAttributesA → canRemovePath → FileUtil.Remove:816
- Stoui 自身是公共入口；仓内无其它调用
- packageName → WriteJson → OutputResult:24 调用 WriteJson
