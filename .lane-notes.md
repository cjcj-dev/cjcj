# lane notes · cjcj#301

规格锚是官方编译器 pin，不是 ZGC。

- 参照 1379951a / 367d9ca8845ba8ef5fd9c17ea983c9692e303f62 `third_party/cmake/BuildCJDB.cmake:7` `cmake_policy(SET CMP0114 NEW)`
- 参照同文件 `STEP_TARGETS build configure`（367d9ca8:183）
- 参照 `src/CMakeLists.txt:385-387` `if(TARGET lldb-build)` / `add_dependencies(lldb-build cangjie-frontend cangjie-lsp-share)`
- 我方落点 `build/srcbuild/stages/fetch.mjs` `applyCompilerCjdbPatches`，调用点是 clone 循环之后；`verifyCompilerCjdbPatches` 仍在 apply 末尾
- 替换对与 f02a939755 删除前的 `BUILDCJDB_EDITS` / `SRC_CMAKE_EDITS` 相同
- `ci/source_pin.env` 的 `COMPILER_REF=174db8f40d5efddee63c47a3162bbf676bc227a0` 不动
- `build/srcbuild/stages/compiler.mjs` 的 `--build-cjdb` 不动
