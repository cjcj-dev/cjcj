# cjcj#161 rework notes

Upstream anchor for R1: `src/Sema/NativeFFI/Utils.cpp:237` calls
`CreateFuncParamList(std::move(lambdaParams))`, which is
`include/cangjie/AST/Create.h:95` (`OwnedPtr` move, `ty = nullptr`).
The one-argument Cangjie overload clones (`Create.cj` `cloneFuncParam`).
`WrapReturningLambdaExpr` must call `CreateFuncParamList(lambdaParams, None)`.

User ctor producer: `AfterTypeCheckASTTransformer.cpp:310-330`.
Our call: `AfterTypeCheckASTTransformer.cj` `TransformToObjCImplUserCtorBody`.
RefExpr target is copied by `CloneRefExpr` (`Clone.cj`), not retargeted, so the
param object in the lambda param list must be the same object.

Cache placement: `DrainGeneratedDecls` inserts `cachedClassDecls` into the file.
A cache-write cut must still leave decls in that map (under a non-lookup key)
or the later compile misses `globalCache`.
