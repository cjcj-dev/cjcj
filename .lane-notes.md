# cjcj#206 notes

上游锚（cangjie_compiler HEAD 367d9ca8845ba8ef5fd9c17ea983c9692e303f62，非 ZGC）：

- InsertStringConversions.cpp:38-49 `IsNSString` 后 `GetMemberDecl<FUNC_DECL>(IsGeneratedNSStringCtor)`，`CJC_NULLPTR_CHECK`。
- InsertStringConversions.cpp:52-59 `IsNSObject` 后 `GetMemberDecl<FUNC_DECL>(IsGeneratedNSObjectToString)`。
- ASTQuery.cpp:276-283 `IsNSString`/`IsNSObject` 走 `NameGenerator::GetObjCDeclName`。
- ASTQuery.cpp:628-636 `IsGeneratedNSStringCtor` 用 `type->symbol->name == STD_LIB_STRING`。谓词不改。

我方：

- AfterTypeCheck.cj InsertStringConversions.HandleImpl：去掉对 `Option<FuncDecl>` 的 `as FuncDecl`，改为 `GetFuncMember(...).getOrThrow()`。
- 分路改为已有 `IsNSString` / `IsNSObject`（ASTQuery.cj）。

# lane notes · cjcj#174

上游锚（cangjie_compiler，含 e9abf014+50ec84e5，不含 e7b33cfd 的 swap）：

- ParseMacro.cpp:48 ParseForeignNodes
- ParseMacro.cpp:76 展开入口把 FOREIGN 修饰符赋给 foreignBlockModifiers（赋值，不 swap）
- ParseMacro.cpp:119 TOPLEVEL && Seeing(FOREIGN) → ParseForeignNodes + continue
- ParseMacro.cpp:125 后续声明 ParseDecl(scope, foreignBlockModifiers)
- ParseMacro.cpp:617 继承修饰符写入 macroCall，只对显式修饰符诊断
- Parser.cpp:346 ParseForeignDecls 赋值 foreignBlockModifiers，:367 clear
- ParseModifiers.cpp:33 SetDeclBeginPos 用 !empty 代替 inForeignBlock
- ParserImpl.cpp:23 Parser::ParseDecl 传入 foreignBlockModifiers
- ParserImpl.cpp:26 SetForeignBlockModifiers 赋值
- Parser.h:129 SetForeignBlockModifiers
- MacroEvaluation.cpp:370 / :671 SetForeignBlockModifiers(macCall.GetModifiers())
- ParserImpl.h:146 数据从 bool inForeignBlock 改为修饰符集合

我方落点：

- packages/parse/src/ParserImpl.cj foreignBlockModifiers
- packages/parse/src/Parser.cj ParseDecl / SetForeignBlockModifiers
- packages/parse/src/ParseDecl.cj ParseForeignDecls / SetDeclBeginPos
- packages/parse/src/ParserUtils.cj ApplyModifiersAndAnnotations（仓颉把 begin 调整拆到这里，同一条件）
- packages/parse/src/ParseMacro.cj ParseForeignNodes / ParseNodesImpl / ParseMacroCall
- packages/macro/src/MacroEvaluation.cj 两处 SetForeignBlockModifiers

不移植 e7b33cfd：ArrayList 可赋值。引用语义下赋值会共享，按 std::set 值拷贝复制元素，不用 swap。

# cjcj#185 rework

上游锚（cangjie_compiler 367d9ca8，非 ZGC）：

- AttributePack.cpp:114 JAVA_CJ_MAPPING(deprecated)
- AttributePack.cpp:119 OBJ_C_CJ_MAPPING(deprecated)
- AttributePack.cpp:124 CJ_MIRROR_JAVA_INTERFACE_FWD(deprecated)
- AttributePack.cpp:132 CJ_MIRROR_JAVA_INTERFACE_DEFAULT(deprecated)
- AttributePack.cpp:137 CJ_MIRROR_OBJC_INTERFACE_FWD(deprecated)
- 枚举项与序数不动。注释不另抄；本仓先例是 MACRO_EXPANDED_NODE(deprecated) 纯显示名。

我方：

- packages/ast/src/AttributePack.cj ATTRIBUTE_NAMES 五个显示名加 (deprecated)
- test/does_not_throw/README.md 不再指向已删选项，改为 finalizer 已随 1e503327 删除

# cjcj#159

上游锚（cangjie_compiler，非 ZGC）：

- 6e125215 Devirtualization.cpp:50 SearchRealCalleeInVtable 按 parent/slot
- 6e125215 Devirtualization.cpp:326 IsValueType（覆盖 a9de4695 的 IsStruct/IsEnum）
- 6e125215 TypeAnalysis.cpp CLASS_STATIC_CAST → HandleClassStaticCastExpr；NUMERIC_CAST / TRY_NUMERIC_CAST 置 Top
- 3d758bbb Devirtualization.cpp AddRefIfNeeded + RewriteToApply 不删上游 cast，GetParamType(0)
- e242ecf4 Devirtualization.cpp:103 CollectUpstreamTypeCasts 只穿 Box/ClassStaticCast
- e242ecf4 Utils.cpp TypeCastOrBoxIfNeeded 返回表达式组；TypeCastIfNeeded 按 IsNumeric 建 NumericCast 或 ClassStaticCast
- e242ecf4 TranslateMemberAccess.cpp TransformThisType 结构体 Load 提前 MoveBefore

我方：

- packages/chir/src/Devirtualization.cj FindUpstreamThisValue / AddRefIfNeeded / RewriteToApply
- packages/chir/src/TypeAnalysis.cj HandleClassStaticCastExpr
- packages/chir/src/ReturnTypeMapCollector.cj CLASS_STATIC_CAST
- packages/chir/src/GenerateVTable.cj TypeCastIfNeeded / GetParamType(0)
- packages/chir/src/Translator.cj TransformThisType

# cjcj#159 rework (review r5840912541)

上游锚（cangjie_compiler e242ecf4，非 ZGC）：

- Devirtualization.cpp:415-416 CreateFunction 第四参 raw mangle 是 ""
- Utils.cpp:511 TypeCastIfNeeded 在 NumericCast/ClassStaticCast 之后 Set<NeedCheckCast>(needCheck)
- Utils.cpp:1755 AddTypeCastForReturnVal 入口 CJC_ASSERT(Is<FuncCall>)；TryApply 经 ApplyBase 属于 FuncCall

我方：

- Devirtualization.cj CreateInstFuncIfPossible rawMangledName: ""
- GenerateVTable.cj TypeCastIfNeeded ret.Set<NeedCheckCast, Bool>(needCheck)
- Utils.cj AddTypeCastForReturnVal 断言 IsApply() || IsTryApply()（#407 把 ApplyWithException 改名为 TryApply；父类仍是 FuncCallWithException，不是 FuncCall；不用 IsFuncCall()）

# cjcj#319 named struct factory guard

Upstream anchor: cangjie_compiler CGType.cpp (read at /root/cj_build/cangjie_compiler).

- GetUnitType: CGType.cpp:377-383. Ours: packages/codegen/src/CGType.cj GetUnitType. LLVMGetTypeByName2 non-null returns; else LLVMStructCreateNamed + empty SetStructTypeBody.
- GetBitMapType: CGType.cpp:411-421. Ours: CGType.cj GetBitMapType. Body stays i32 + [0 x i8].
- GetOrCreateTypeTemplateType: CGType.cpp:525-549. Ours: CGType.cj. 11 fields stay CGCommonDef.cj TypeTemplateFields order.
- GetOrCreateGenericTypeInfoType: CGType.cpp:906-918, public at CGType.h:282. Ours: packages/codegen/src/CGTypeInfo.cj, visibility public.
- GetOrCreateGenericCustomTypeInfoType: CGType.cpp:921-936, public at CGType.h:283. Ours: CGTypeInfo.cj, visibility public.

Positive already in tree: GetOrCreateTypeInfoType, GetArrayBaseType, GetOrCreateExtensionDefType.
LLVM precondition: llvm_rebase llvm/lib/IR/Type.cpp:446 assert(isOpaque() && "Struct body already set!").
Do not change ExtensionDef (cjcj#74), Func.Type, struct._param, or field order.
Consumer observed by the harness: GetOrCreateTypeInfoType GetBitMapType call (TYPEINFO_GC_TIB, body index 5).

# cjcj#321 rework

规格锚（非 ZGC）：

- /root/cj_build/cangjie_compiler/third_party/CMakeLists.txt:199 `-DLLVM_ENABLE_LIBXML2=OFF`
- ci/platform_tuples/build_tuple.sh:37 同一开关
- llvm 501694e llvm/lib/WindowsManifest/CMakeLists.txt:3-4 仅在该开关打开时链 LibXml2
- ci/release/language_tuple.py ROLES 与 validate_provenance 的 stdlib inputs 同一列

我方：

- .github/workflows/build-llvm-tools.yml 与 tools/srcbuild_kkk2.sh 的色库 cmake 加上同一开关，链接后走 ci/assert_no_libxml2_needed.sh
- language_tuple.py ROLES 增加 ld.lld；H48 发布 396986820 重钉已有 official-sdk 字节，不换成色库链接器
- 色库重发 run 36202946134 artifact 10893737926；bin/ld.lld sha fdc258dcbdadd9f14ca16d9ea77099572c3d52657a8611ed40ce3a375c1f7425，readelf 无 libxml2

# cjcj#321 rework r5841782886

审查打回：tools/srcbuild_kkk2.sh 对构建树 ld.lld 直接跑 assert_no_libxml2_needed.sh，而该路径是符号链接。

规格锚（非 ZGC）：

- lld/tools/lld/CMakeLists.txt:31 add_lld_symlink(ld.lld lld)
- llvm/cmake/modules/AddLLVM.cmake:2115-2119 UNIX 上 LLVM_LINK_OR_COPY=create_symlink；:2132-2133 POST_BUILD 建链
- .github/workflows/build-llvm-tools.yml:278-290 发布检查打在 gunzip 重定向出的普通文件上，不打构建树链接
- ci/assert_no_libxml2_needed.sh:8-11 仍拒绝符号链接；readelf 本身跟随链接，-L 拒绝没有配方锚，所以不解检查器

我方：

- tools/srcbuild_kkk2.sh build_fixed_tuple 在调用检查器前 readlink -f，三枚工具同一写法
- 测试走抽出的 build_fixed_tuple 本体：符号链接干净目标 rc=0 且打印 NO_LIBXML2 lld；符号链接坏目标 rc=1 且 DT_NEEDED 路径以 /lld 结尾；检查器直接打 ld.lld 仍 rc=2

