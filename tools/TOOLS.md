# Infrastructure tool index

- `reprobank.sh`: collect classified report repros into `tests/repros/`, then run them against a selected compiler.
- `megascan.sh`: merge an ordered branch pool in a disposable worktree and print conflict/overlap tables.
- `autobaseline.sh`: rebuild a commit with copied runtime shims, run the 15-package sc_bcgate suite, and append the baseline trend.
