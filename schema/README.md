# FlatBuffers schemas

`MacroMsgFormat.fbs` mirrors `cangjie_compiler/schema/MacroMsgFormat.fbs` verbatim. After updating the reference checkout, verify synchronization with `cmp -s schema/MacroMsgFormat.fbs /root/cj_build/cangjie_compiler/schema/MacroMsgFormat.fbs`.
