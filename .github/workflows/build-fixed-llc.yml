# Build the -O2-fixed llc from source (no binaries in git; task#15).
# The stock nightly llc miscompiles cjcj's relocate-of-undef at -O2 (materializes a
# 0xFEFEFEFE sentinel into a callee-saved GC-root register -> runtime GC SIGSEGV).
# The fix (getUNDEF lowering) lives in the cjcj_llvm fork. As of fork tip 4f61703c the
# whole llc builds PURE from source: the Cangjie demangler is an in-tree passthrough stub,
# so no prebuilt libcangjie-demangle.a and no CANGJIE_DEMANGLE_DIR are needed. Cache the
# result keyed by the fork commit so the ~1h cold build runs only when the fork changes.
# Recipe: reports/llvmfix/getundef/CI_LLC_FROM_SOURCE.md
name: build-fixed-llc

on:
  push:
    branches: [debug/llc-build]
  workflow_call:
    outputs:
      cache-key:
        description: actions/cache key holding fixed-llc/llc.gz
        value: ${{ jobs.build-llc.outputs.cache-key }}

permissions:
  contents: read

env:
  FORK_URL: https://github.com/cjcj-dev/cjcj_llvm.git
  FORK_REF: fix/scheddag-memcorrupt
  # tip = getUNDEF (2b96e718) + IRBuilder<> clang-CTAD fix (5ca4a192)
  #     + demangler-from-source stub (4f61703c, drops the prebuilt .a)
  FORK_SHA: 4f61703c3225d60a7dc839718d822911628e6740

jobs:
  build-llc:
    name: build fixed llc (x86_64)
    # ubuntu-22.04 = glibc 2.35 + clang-14 (llvmfix's validated build config; the
    # -include list was tuned for clang-14). Building here yields a portable llc that
    # loads on all cjcj build runners (22.04/24.04/26.04) — a 24.04-built llc links
    # glibc 2.38 and fails to load on the 22.04 gating build job.
    runs-on: ubuntu-22.04
    timeout-minutes: 120
    outputs:
      cache-key: ${{ steps.key.outputs.key }}
    steps:
      - name: Compute cache key
        id: key
        # glibc2.35 = built on 22.04 so the llc runs on every build runner
        # (22.04/24.04/26.04); a 24.04-built llc links glibc 2.38 and cannot
        # load on the 22.04 gating build job.
        run: echo "key=fixed-llc-x86_64-glibc2.35-${FORK_SHA}" >> "$GITHUB_OUTPUT"

      - name: Restore cached llc
        id: cache
        uses: actions/cache@v4
        with:
          path: fixed-llc/llc.gz
          key: ${{ steps.key.outputs.key }}

      - name: Install build deps
        if: steps.cache.outputs.cache-hit != 'true'
        run: sudo apt-get update -qq && sudo apt-get install -y -qq clang ninja-build cmake

      - name: Checkout llvm fork @ fixed commit
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          git clone --filter=blob:none --branch "$FORK_REF" "$FORK_URL" llvm-fork
          git -C llvm-fork checkout "$FORK_SHA"
          test "$(git -C llvm-fork rev-parse HEAD)" = "$FORK_SHA"

      - name: Configure + build llc (pure source, in-tree demangle stub)
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          cmake -G Ninja -B llc-build \
            -DCMAKE_BUILD_TYPE=Release \
            -DLLVM_ENABLE_ASSERTIONS=OFF \
            -DBUILD_SHARED_LIBS=OFF \
            -DLLVM_ENABLE_RTTI=OFF \
            -DLLVM_TARGETS_TO_BUILD=X86 \
            -DLLVM_ENABLE_PROJECTS="" \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_CXX_COMPILER=clang++ \
            -DCMAKE_CXX_FLAGS="-gline-tables-only -include cstdint -include unordered_map -include map -include vector -include string" \
            llvm-fork/llvm
          ninja -C llc-build llc
          mkdir -p fixed-llc
          gzip -c -9 llc-build/bin/llc > fixed-llc/llc.gz

      - name: Verify llc runs
        run: |
          gunzip -c fixed-llc/llc.gz > /tmp/llc && chmod +x /tmp/llc
          /tmp/llc --version | head -5
          echo "fixed llc.gz sha256(top16): $(sha256sum fixed-llc/llc.gz | cut -c1-16)"

      - name: Upload llc artifact
        uses: actions/upload-artifact@v4
        with:
          name: fixed-llc-x86_64
          path: fixed-llc/llc.gz
          retention-days: 7
