name: Patched cjpm for Windows

on:
  workflow_dispatch:
  workflow_call:
    secrets:
      GITCODE_API_KEY:
        required: false

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  build-cjpm:
    name: patched cjpm / windows_x86_64 / windows-2022
    runs-on: windows-2022
    timeout-minutes: 90

    steps:
      - name: Checkout cjcj
        uses: actions/checkout@v7

      - name: Load cjpm pin
        id: cjpm-pin
        run: |
          cat ci/cjpm_pin.env >> "$GITHUB_ENV"
          source ci/cjpm_pin.env
          echo "ref=$CJPM_FORK_REF" >> "$GITHUB_OUTPUT"
          echo "toolchain=$CJCJ_TOOLCHAIN" >> "$GITHUB_OUTPUT"

      - name: Restore patched cjpm cache
        id: cjpm-cache
        uses: actions/cache@v6
        with:
          path: patched-cjpm/windows_x86_64
          key: cjpm-win-${{ steps.cjpm-pin.outputs.ref }}-${{ steps.cjpm-pin.outputs.toolchain }}

      - name: Prepare MSYS2/MinGW build toolchain
        if: steps.cjpm-cache.outputs.cache-hit != 'true'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          path-type: inherit
          install: >-
            git gzip python cmake make
            mingw-w64-x86_64-clang
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-libc++
            mingw-w64-x86_64-libunwind
            mingw-w64-x86_64-make
            mingw-w64-x86_64-python

      - name: Install pinned Cangjie SDK with stdx
        if: steps.cjpm-cache.outputs.cache-hit != 'true'
        shell: pwsh
        env:
          GITCODE_API_KEY: ${{ secrets.GITCODE_API_KEY }}
        run: |
          $cjvRoot = Join-Path $env:USERPROFILE '.local\bin'
          $archive = Join-Path $env:RUNNER_TEMP 'cjv_windows_amd64.zip'
          $extract = Join-Path $env:RUNNER_TEMP 'cjv-windows'
          New-Item -ItemType Directory -Force $cjvRoot | Out-Null
          Invoke-WebRequest -Uri 'https://github.com/Zxilly/cjv/releases/download/v0.2.20/cjv_windows_amd64.zip' -OutFile $archive
          Expand-Archive -LiteralPath $archive -DestinationPath $extract -Force
          $downloaded = Get-ChildItem -Path $extract -Filter cjv.exe -Recurse | Select-Object -First 1
          if (-not $downloaded) { throw 'cjv.exe missing from downloaded archive' }
          Copy-Item -LiteralPath $downloaded.FullName -Destination (Join-Path $cjvRoot 'cjv.exe') -Force
          $env:PATH = "$cjvRoot;$env:PATH"
          if ($env:GITCODE_API_KEY) { cjv set gitcode-api-key $env:GITCODE_API_KEY }
          cjv install $env:CJCJ_TOOLCHAIN -c stdx
          $cjvRoot | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Fetch pinned cjpm fork
        if: steps.cjpm-cache.outputs.cache-hit != 'true'
        shell: msys2 {0}
        run: |
          git clone --filter=blob:none --no-checkout "$CJPM_FORK_URL" cangjie-tools
          git -C cangjie-tools checkout --detach "$CJPM_FORK_REF"
          test "$(git -C cangjie-tools rev-parse HEAD)" = "$CJPM_FORK_REF"

      - name: Build patched cjpm
        if: steps.cjpm-cache.outputs.cache-hit != 'true'
        shell: msys2 {0}
        run: |
          set -euxo pipefail
          cangjie_home="$(cygpath -u "$USERPROFILE/.cjv/toolchains/$CJCJ_TOOLCHAIN")"
          stdx_path="$(cygpath -u "$USERPROFILE/.cjv/stdx/$CJCJ_TOOLCHAIN/static/stdx")"
          export CANGJIE_HOME="$cangjie_home"
          export CANGJIE_STDX_PATH="$stdx_path"
          export MINGW_PATH=/mingw64
          export cjHeapSize=12GB
          # cjpm is itself a Cangjie binary: it loads libcangjie-runtime.dll (and
          # tools/lib deps) at startup — r3 forensics: found on PATH, exec rc=127.
          export PATH="$cangjie_home/bin:$cangjie_home/tools/bin:$cangjie_home/runtime/lib/windows_x86_64_cjnative:$cangjie_home/tools/lib:/mingw64/bin:$PATH"
          # r2 died here with a bare 127 and no output — name the failing layer.
          ls "$cangjie_home" "$cangjie_home/tools/bin" 2>&1 | head -40 || true
          command -v cjpm || echo "cjpm NOT on PATH"
          cjpm --version || echo "cjpm --version rc=$?"
          # r5/r6: the msys2-built libuv/fswatcher and msys2 libc++ expect the
          # msys2 CRT generation (__hash_memory, __imp_btowc, __sys_nerr…) but
          # the SDK's older bundled mingw layer wins the -L order. Swap the
          # whole C/C++ runtime layer to the single msys2 generation —
          # build-local only, nothing here ships.
          for lib in libc++.a libc++abi.a libc++experimental.a libunwind.a \
                     libmsvcrt.a libmingwex.a libmingw32.a libmoldname.a libpthread.a libm.a \
                     crt2.o crtbegin.o crtend.o CRT_glob.o; do
            for sdkcopy in $(find "$cangjie_home/third_party" -name "$lib" 2>/dev/null); do
              test -f "/mingw64/lib/$lib" || continue
              cp -v "/mingw64/lib/$lib" "$sdkcopy"
            done
          done
          cd cangjie-tools/cjpm
          cjpm build
          product=target/release/bin/main
          test -f "$product" || product=target/release/bin/main.exe
          test -f "$product"
          mkdir -p ../../patched-cjpm/windows_x86_64
          cp "$product" ../../patched-cjpm/windows_x86_64/cjpm.exe
          ../../patched-cjpm/windows_x86_64/cjpm.exe --version

      - name: Verify patched cjpm payload
        run: |
          node ci/release/prepare_cjpm_artifact.mjs \
            --platform windows-x64 \
            --binary patched-cjpm/windows_x86_64/cjpm.exe \
            --outdir "$RUNNER_TEMP/cjpm-provenance"
          cp "$RUNNER_TEMP/cjpm-provenance/cjpm.exe" patched-cjpm/windows_x86_64/cjpm.exe
          gzip -n -c -9 patched-cjpm/windows_x86_64/cjpm.exe > patched-cjpm/windows_x86_64/cjpm.exe.gz
          cp "$RUNNER_TEMP/cjpm-provenance/CJPM-PROVENANCE.json" \
            patched-cjpm/windows_x86_64/CJPM-PROVENANCE.json
          test -s patched-cjpm/windows_x86_64/cjpm.exe.gz
          test -s patched-cjpm/windows_x86_64/CJPM-PROVENANCE.json

      - name: Upload patched cjpm
        uses: actions/upload-artifact@v7
        with:
          name: patched-cjpm-windows_x86_64
          path: |
            patched-cjpm/windows_x86_64/cjpm.exe.gz
            patched-cjpm/windows_x86_64/CJPM-PROVENANCE.json
          if-no-files-found: error
          retention-days: 14
