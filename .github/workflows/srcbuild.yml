name: Self-hosted source SDK and final std

on:
  workflow_call:
    inputs:
      cangjie_ref:
        description: Optional SDK version label; source repository revisions remain pinned
        required: false
        type: string
        default: ''
      build_type:
        description: C++ SDK build type for native hosts
        required: false
        type: string
        default: relwithdebinfo
      dry_run:
        description: Authorize an exact runtime_ref override without publishing
        required: false
        type: boolean
        default: false
      language_tuple:
        description: Build the source language tuple from a same-run runtime artifact (Linux x64 only)
        required: false
        type: boolean
        default: false
      runtime_artifact_id:
        description: Same-run compiler runtime artifact ID for language_tuple
        required: false
        type: string
        default: ''
      runtime_manifest_sha256:
        description: Same-run compiler runtime manifest digest for language_tuple
        required: false
        type: string
        default: ''
      runtime_ref:
        description: Exact runtime commit for dry-run or language_tuple; otherwise ci/runtime_pin.env is used
        required: false
        type: string
        default: ''
      targets:
        description: >-
          Comma-separated source targets to build; "all" builds every one. A
          release phase passes its own target so it pays for that target and no
          other.
        required: false
        type: string
        default: all
      rebuild_llvm:
        description: Bypass the whole-artifact LLVM tools cache so the LLVM sccache hit rate can be measured.
        required: false
        type: boolean
        default: false
    outputs:
      final_compiler_linux_x64:
        description: Same-run final compiler for linux-x64
        value: ${{ jobs.artifact-contract.outputs.final_compiler_linux_x64 }}
      final_std_linux_x64:
        description: Linux x64 source-built std artifact
        value: ${{ jobs.artifact-contract.outputs.final_std_linux_x64 }}
      final_compiler_linux_aarch64:
        description: Same-run final compiler for linux-aarch64
        value: ${{ jobs.artifact-contract.outputs.final_compiler_linux_aarch64 }}
      final_std_linux_aarch64:
        description: Linux AArch64 source-built std artifact
        value: ${{ jobs.artifact-contract.outputs.final_std_linux_aarch64 }}
      final_compiler_darwin_x64:
        description: Same-run final compiler for darwin-x64
        value: ${{ jobs.artifact-contract.outputs.final_compiler_darwin_x64 }}
      final_std_darwin_x64:
        description: Darwin x64 source-built std artifact
        value: ${{ jobs.artifact-contract.outputs.final_std_darwin_x64 }}
      final_compiler_darwin_arm64:
        description: Same-run final compiler for darwin-arm64
        value: ${{ jobs.artifact-contract.outputs.final_compiler_darwin_arm64 }}
      final_std_darwin_arm64:
        description: Darwin arm64 source-built std artifact
        value: ${{ jobs.artifact-contract.outputs.final_std_darwin_arm64 }}
      final_std_windows_x64:
        description: Windows x64 source-built std artifact
        value: ${{ jobs.artifact-contract.outputs.final_std_windows_x64 }}
  workflow_dispatch:
    inputs:
      cangjie_ref:
        description: Optional SDK version label; source repository revisions remain pinned
        required: false
        type: string
      build_type:
        description: C++ SDK build type
        required: true
        default: relwithdebinfo
        type: choice
        options: [release, relwithdebinfo, debug]
      dry_run:
        description: Authorize an exact runtime_ref override without publishing
        required: false
        type: boolean
        default: false
      language_tuple:
        description: Build the source language tuple from a same-run runtime artifact (Linux x64 only)
        required: false
        type: boolean
        default: false
      runtime_artifact_id:
        description: Same-run compiler runtime artifact ID for language_tuple
        required: false
        type: string
        default: ''
      runtime_manifest_sha256:
        description: Same-run compiler runtime manifest digest for language_tuple
        required: false
        type: string
        default: ''
      runtime_ref:
        description: Exact runtime commit for dry-run or language_tuple; otherwise ci/runtime_pin.env is used
        required: false
        type: string
        default: ''
      targets:
        description: >-
          Comma-separated source targets to build; "all" builds every one. A
          release phase passes its own target so it pays for that target and no
          other.
        required: false
        type: string
        default: all
      rebuild_llvm:
        description: Bypass the whole-artifact LLVM tools cache so the LLVM sccache hit rate can be measured.
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  actions: read

# Per target set, not per ref alone: release-matrix.yml calls this workflow once
# per platform in the same run, and one group for all of them would leave every
# call but one pending -- and cancel all but the newest pending one.
concurrency:
  group: srcbuild-native-${{ github.ref }}-${{ inputs.targets || 'all' }}
  cancel-in-progress: false

jobs:
  # Four source SDK builds are the expensive part of a release, and starting all
  # of them before phase 1 has reported anything is what the phase policy exists
  # to prevent. Selecting here lets each phase call this workflow for its own
  # target; the tuple list stays in one place.
  plan:
    name: Select targets
    runs-on: ubuntu-24.04
    outputs:
      matrix: ${{ steps.select.outputs.matrix }}
      llvm_platforms: ${{ steps.select.outputs.llvm_platforms }}
    steps:
      - name: Select the requested targets
        id: select
        env:
          REQUESTED: ${{ inputs.targets }}
        run: |
          set -euo pipefail
          all='[
            {"target":"linux-aarch64","runner":"ubuntu-24.04-arm","llvm_platform":"linux_aarch64","static_libs":true},
            {"target":"darwin-arm64","runner":"macos-15","llvm_platform":"darwin_aarch64","static_libs":false},
            {"target":"darwin-x64","runner":"macos-15-intel","llvm_platform":"darwin_x86_64","static_libs":false},
            {"target":"linux-x64","runner":"ubuntu-22.04","llvm_platform":"linux_x86_64","static_libs":true}
          ]'
          if [ -z "$REQUESTED" ] || [ "$REQUESTED" = all ]; then
            selected="$all"
          else
            selected="$(printf '%s' "$all" | jq -c --arg want "$REQUESTED" \
              '[.[] | select(.target as $t | ($want | split(",") | map(gsub("^ +| +$";"")) | index($t)))]')"
          fi
          count="$(printf '%s' "$selected" | jq 'length')"
          # An empty matrix skips every dependent job while reporting nothing wrong.
          if [ "$count" -eq 0 ]; then
            echo "no target matched targets=$REQUESTED" >&2
            exit 1
          fi
          echo "selected $count target(s) for targets=$REQUESTED"
          printf 'matrix=%s\n' "$(printf '%s' "$selected" | jq -c '{include: .}')" >> "$GITHUB_OUTPUT"
          printf 'llvm_platforms=%s\n' "$(printf '%s' "$selected" | jq -r '[.[].llvm_platform] | join(",")')" >> "$GITHUB_OUTPUT"

  fixed-llvm:
    name: Build native LLVM tools
    needs: plan
    uses: ./.github/workflows/build-llvm-tools.yml
    with:
      platforms: ${{ needs.plan.outputs.llvm_platforms }}
      rebuild: ${{ inputs.rebuild_llvm || false }}

  srcbuild:
    name: ${{ matrix.target }} / source SDK
    needs: [plan, fixed-llvm]
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 360
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.plan.outputs.matrix) }}
    env:
      CANGJIE_WORKSPACE: ${{ github.workspace }}/.srcbuild/workspace
      CANGJIE_BUILD_ROOT: ${{ github.workspace }}/.srcbuild/buildtools
      CJCJ_SRCBUILD_TARGET: ${{ matrix.target }}
      CJCJ_FIXED_LLVM_DIR: ${{ github.workspace }}/.srcbuild/fixed-llc
      # sccache: .github/actions/sccache below. The disk cache is persisted as one
      # actions/cache entry per (component, target, pin); the sccache GitHub
      # Actions backend that used to be considered here wrote one entry per
      # object file and was measured at 3% hits (run 31551077927: 27840 entries,
      # 6.9 GB of the 10 GB cap, 4162 s of writes). That measurement is why the
      # backend is a tarball and not SCCACHE_GHA_ENABLED.
      cjHeapSize: 12GB
      cjHeapSwap: "on"
      CJCJ_STAGE3_STDLIB_BUILD_TYPE: ${{ inputs.build_type || 'relwithdebinfo' }}
    steps:
      - name: Checkout cjcj
        uses: actions/checkout@v7

      - name: Load host toolchain pin
        run: cat ci/host_sdk_pin.env >> "$GITHUB_ENV"

      - name: Load source pins
        run: cat ci/source_pin.env >> "$GITHUB_ENV"

      - name: Load and validate runtime pin
        env:
          CJCJ_RUNTIME_REF_OVERRIDE: ${{ inputs.runtime_ref }}
          CJCJ_ALLOW_RUNTIME_OVERRIDE: ${{ (inputs.dry_run || inputs.language_tuple) && inputs.runtime_ref != '' }}
        run: npx --yes zx@8 ci/load_runtime_pin.mjs

      - name: Provision uncoloured host SDK
        shell: bash
        run: |
          set -euo pipefail
          export CJCJ_SDK_STOCK_LLC=1
          npx --yes zx@8 ci/setup_sdk.mjs
          host_sdk="$HOME/.cjv/toolchains/$CJCJ_TOOLCHAIN"
          test -d "$host_sdk"
          echo "CJCJ_SRCBUILD_BOOTSTRAP_SDK=$host_sdk" >> "$GITHUB_ENV"
          echo "CJCJ_SRCBUILD_HOST_SDK=$host_sdk" >> "$GITHUB_ENV"

      - name: Install build dependencies
        run: npx --yes zx@8 build/cli.mjs --target "${{ matrix.target }}" install-system-deps

      # Every C/C++ compile in this job -- support libraries, stage3 std FFI,
      # stdx and tools cmake projects, the compiler shim, the Windows cross
      # runtime -- runs through this launcher; build/toolchain/sccache.mjs keeps
      # the launcher variables it finds already set.
      - name: Start sccache
        uses: ./.github/actions/sccache
        with:
          component: srcbuild
          platform: ${{ matrix.target }}
          pin: ${{ env.RUNTIME_REF }}
          cache-size: 2G

      - name: Restore support library cache
        if: matrix.static_libs
        id: support-cache
        uses: actions/cache/restore@v6
        with:
          path: ${{ env.CANGJIE_BUILD_ROOT }}
          key: cangjie-srcbuild-support-${{ matrix.target }}-${{ hashFiles('build/toolchain/static-libs.mjs', 'build/lib/archive.mjs', 'build/lib/runner.mjs') }}

      - name: Build support libraries
        if: matrix.static_libs && steps.support-cache.outputs.cache-hit != 'true'
        run: npx --yes zx@8 build/cli.mjs --target "${{ matrix.target }}" install-static-libs

      - name: Save support library cache
        if: matrix.static_libs && steps.support-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v6
        with:
          path: ${{ env.CANGJIE_BUILD_ROOT }}
          key: cangjie-srcbuild-support-${{ matrix.target }}-${{ hashFiles('build/toolchain/static-libs.mjs', 'build/lib/archive.mjs', 'build/lib/runner.mjs') }}

      # --target because build/cli.mjs asserts the host contract of its target
      # before any command, and the default target is linux-x64: on the
      # ubuntu-24.04-arm cell this step failed with "target linux-x64 required
      # host linux/x64; current host linux/arm64" (release-matrix run 35717546067).
      - name: Generate SDK version
        id: version
        env:
          REQUESTED_REF: ${{ inputs.cangjie_ref }}
        run: |
          value=$(npx --yes zx@8 build/cli.mjs --target "${{ matrix.target }}" --cangjie-version "$REQUESTED_REF" print-version)
          echo "value=$value" >> "$GITHUB_OUTPUT"

      - name: Download pinned source
        run: |
          npx --yes zx@8 build/cli.mjs \
            --target "${{ matrix.target }}" \
            --build-type "${{ inputs.build_type || 'relwithdebinfo' }}" \
            --cangjie-version "${{ steps.version.outputs.value }}" \
            fetch \
            --repo-url "compiler=$COMPILER_SRC_URL" \
            --repo-tag "compiler=$COMPILER_REF" \
            --repo-url "runtime=$RUNTIME_SRC_URL" \
            --repo-tag "runtime=$RUNTIME_REF" \
            --repo-url "tools=$TOOLS_SRC_URL" \
            --repo-tag "tools=$TOOLS_REF" \
            --repo-url "stdx=$STDX_SRC_URL" \
            --repo-tag "stdx=$STDX_REF"

      - name: Verify source revisions
        run: npx --yes zx@8 ci/srcbuild/steps/verify-source-pins.mjs

      - name: Download native LLVM tuple
        uses: actions/download-artifact@v8
        with:
          name: fixed-llvm-tools-${{ matrix.llvm_platform }}
          path: ${{ env.CJCJ_FIXED_LLVM_DIR }}

      - name: Load immutable bootstrap host LLVM provenance
        if: matrix.target == 'linux-x64'
        shell: bash
        run: |
          set -euo pipefail
          node ci/release/host_llvm.mjs env >> "$GITHUB_ENV"
          echo "CJCJ_BOOTSTRAP_HOST_LLVM_ARTIFACT=$RUNNER_TEMP/bootstrap-host-llvm" >> "$GITHUB_ENV"

      - name: Download pinned bootstrap host LLVM
        if: matrix.target == 'linux-x64'
        uses: actions/download-artifact@v8
        with:
          artifact-ids: ${{ env.HOST_LLVM_ARTIFACT_ID }}
          merge-multiple: true
          github-token: ${{ github.token }}
          run-id: ${{ env.HOST_LLVM_RUN_ID }}
          path: ${{ env.CJCJ_BOOTSTRAP_HOST_LLVM_ARTIFACT }}

      - name: Load immutable in-process LLVM dylib provenance
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          source ci/llvm_pin.env
          source "ci/llvm-dylib/${{ matrix.llvm_platform }}.env"
          test "$LLVM_DYLIB_SOURCE_SHA" = "$LLVM_SHA"
          test -n "$LLVM_DYLIB_RUN_ID"
          test -n "$LLVM_DYLIB_RUN_ATTEMPT"
          test -n "$LLVM_DYLIB_ARTIFACT_ID"
          cat "ci/llvm-dylib/${{ matrix.llvm_platform }}.env" >> "$GITHUB_ENV"
          echo "CJCJ_BOOTSTRAP_DYLIB_ARTIFACT=$RUNNER_TEMP/llvm-depot/$LLVM_SHA/$CANGJIE_COMPILER_SHA/dylib" >> "$GITHUB_ENV"

      - name: Download pinned in-process LLVM dylib
        if: runner.os == 'Linux'
        uses: actions/download-artifact@v8
        with:
          artifact-ids: ${{ env.LLVM_DYLIB_ARTIFACT_ID }}
          merge-multiple: true
          github-token: ${{ github.token }}
          run-id: ${{ env.LLVM_DYLIB_RUN_ID }}
          path: ${{ env.CJCJ_BOOTSTRAP_DYLIB_ARTIFACT }}

      - name: Load pinned colour runtime provenance
        shell: bash
        env:
          RUNTIME_PLATFORM: ${{ matrix.llvm_platform }}
          BUILD_LANGUAGE_TUPLE: ${{ inputs.language_tuple }}
          RUNTIME_ARTIFACT_ID: ${{ inputs.runtime_artifact_id }}
          RUNTIME_MANIFEST_SHA256: ${{ inputs.runtime_manifest_sha256 }}
        run: |
          set -euo pipefail
          pin="ci/colour-runtime/$RUNTIME_PLATFORM.env"
          test -f "$pin" || { echo "COLOUR_RT_PLATFORM_PIN_MISSING: $RUNTIME_PLATFORM" >&2; exit 1; }
          if [ "$BUILD_LANGUAGE_TUPLE" = true ]; then
            test "$RUNTIME_PLATFORM" = linux_x86_64
            test -n "$RUNTIME_REF"
            [[ "$RUNTIME_ARTIFACT_ID" =~ ^[0-9]+$ ]]
            [[ "$RUNTIME_MANIFEST_SHA256" =~ ^[0-9a-f]{64}$ ]]
            {
              echo "COLOUR_RT_RUN_ID=$GITHUB_RUN_ID"
              echo "COLOUR_RT_RUN_ATTEMPT=$GITHUB_RUN_ATTEMPT"
              echo "COLOUR_RT_ARTIFACT_ID=$RUNTIME_ARTIFACT_ID"
              echo "COLOUR_RT_MANIFEST_SHA256=$RUNTIME_MANIFEST_SHA256"
            } >> "$GITHUB_ENV"
          else
            cat "$pin" >> "$GITHUB_ENV"
          fi
          echo "CJCJ_BOOTSTRAP_COLOUR_RT=$RUNNER_TEMP/colour-runtime" >> "$GITHUB_ENV"

      - name: Download pinned colour runtime pair
        uses: actions/download-artifact@v8
        with:
          artifact-ids: ${{ env.COLOUR_RT_ARTIFACT_ID }}
          merge-multiple: true
          github-token: ${{ github.token }}
          run-id: ${{ env.COLOUR_RT_RUN_ID }}
          path: ${{ env.CJCJ_BOOTSTRAP_COLOUR_RT }}

      - name: Inspect downloaded colour runtime symbols
        shell: bash
        run: |
          set -euo pipefail
          so="$CJCJ_BOOTSTRAP_COLOUR_RT/runtime/lib/linux_x86_64_cjnative/libcangjie-runtime.so"
          archive="$CJCJ_BOOTSTRAP_COLOUR_RT/lib/linux_x86_64_cjnative/libcangjie-runtime.a"
          sha256sum "$so" "$archive"
          nm --defined-only "$so" > "$RUNNER_TEMP/colour-runtime-symbols.txt"
          awk '$NF ~ /^g_cjLoadBadMask(@@?[^[:space:]]+)?$/ {print; n++} END {print "COLOUR_RT_DEFINED_MASK_COUNT=" n+0; exit n != 1}' "$RUNNER_TEMP/colour-runtime-symbols.txt"

      - name: Load immutable AST support provenance
        shell: bash
        env:
          AST_PLATFORM: ${{ matrix.llvm_platform }}
        run: |
          set -euo pipefail
          # shellcheck source=/dev/null
          source "ci/ast_support/${AST_PLATFORM}.env"
          test -n "$AST_SUPPORT_RUN_ID"
          test -n "$AST_SUPPORT_RUN_ATTEMPT"
          test -n "$AST_SUPPORT_ARTIFACT_ID"
          cat "ci/ast_support/${AST_PLATFORM}.env" >> "$GITHUB_ENV"

      - name: Download AST support archive
        uses: actions/download-artifact@v8
        with:
          artifact-ids: ${{ env.AST_SUPPORT_ARTIFACT_ID }}
          merge-multiple: true
          github-token: ${{ github.token }}
          run-id: ${{ env.AST_SUPPORT_RUN_ID }}
          path: ${{ runner.temp }}/ast-support

      # set -a: `source` alone makes shell variables, and prepare_bootstrap_inputs
      # reads LLVM_SHA from its environment -- without the export every host
      # failed here with "LLVM_SHA pin missing" (release-matrix run 35718484244).
      - name: Resolve bootstrap inputs on this runner
        env:
          GITHUB_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          set -a
          source ci/llvm_pin.env
          selected_runtime_ref="$RUNTIME_REF"
          source ci/runtime_pin.env
          export RUNTIME_REF="$selected_runtime_ref"
          set +a
          CJCJ_BOOTSTRAP_AST_ARTIFACT="$RUNNER_TEMP/ast-support/libcangjie-ast-support.a" \
            node ci/release/prepare_bootstrap_inputs.mjs

      - name: Prove colour runtime identity rejection
        shell: bash
        run: |
          set -euo pipefail
          set -a
          source ci/llvm_pin.env
          selected_runtime_ref="$RUNTIME_REF"
          source ci/runtime_pin.env
          export RUNTIME_REF="$selected_runtime_ref"
          set +a
          first=0
          if [ "${COLOUR_RT_MANIFEST_SHA256:0:1}" = 0 ]; then first=1; fi
          wrong="$first${COLOUR_RT_MANIFEST_SHA256:1}"
          set +e
          COLOUR_RT_MANIFEST_SHA256="$wrong" GITHUB_ENV="$RUNNER_TEMP/invalid-runtime.env" \
            node ci/release/prepare_bootstrap_inputs.mjs > "$RUNNER_TEMP/invalid-runtime.log" 2>&1
          rc=$?
          set -e
          test "$rc" = 1
          grep -F "COLOUR_RT_SHA256_MISMATCH expected=$wrong actual=$COLOUR_RT_MANIFEST_SHA256" "$RUNNER_TEMP/invalid-runtime.log"
          test ! -s "$RUNNER_TEMP/invalid-runtime.env"
          echo "ASSERT colour-runtime wrong-pin rejected rc=$rc; valid input resolved in prior step"

      - name: Reject a one-digit host pin change with the real artifact
        if: matrix.target == 'linux-x64'
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import os, re
          from pathlib import Path
          original = Path('ci/bootstrap/stage1_host_identities.txt').read_text()
          changed, count = re.subn(r'^(libLLVM-15.so )([a-f0-9])',
              lambda m: m[1] + ('1' if m[2] == '0' else '0'), original, flags=re.M)
          assert count == 1
          Path(os.environ['RUNNER_TEMP'], 'host-pin-cut.txt').write_text(changed)
          PY
          sha256sum ci/bootstrap/stage1_host_identities.txt "$RUNNER_TEMP/host-pin-cut.txt"
          if STAGE1_HOST_IDENTITIES="$RUNNER_TEMP/host-pin-cut.txt" \
              node ci/release/prepare_bootstrap_inputs.mjs > "$RUNNER_TEMP/host-pin-cut.log" 2>&1; then
            echo 'host pin change unexpectedly accepted' >&2
            exit 1
          else
            echo "HOST_PIN_CUT_RC=$?"
          fi
          grep -F 'HOST_LLVM_SHA256_MISMATCH expected=' "$RUNNER_TEMP/host-pin-cut.log"
          echo 'ASSERT real-artifact host pin one-digit rejection executed'

      - name: Record host repair and nightly control symbols
        if: matrix.target == 'linux-x64'
        shell: bash
        run: |
          set -euo pipefail
          for arm in repaired nightly; do
            if [ "$arm" = repaired ]; then
              so="$CJCJ_BOOTSTRAP_HOST_LLVM_SO"
            else
              so="$CJCJ_BOOTSTRAP_BASE/third_party/llvm/lib/libLLVM-15.so"
            fi
            sha256sum "$so"
            nm --defined-only "$so" > "$RUNNER_TEMP/host-$arm.nm"
            c++filt < "$RUNNER_TEMP/host-$arm.nm" > "$RUNNER_TEMP/host-$arm.symbols"
            awk -v arm="$arm" 'index($0, "llvm::SelectionDAGBuilder::visitRelocate(") {print; n++} END {print "HOST_RELOCATE_SYMBOLS arm=" arm " count=" n+0}' "$RUNNER_TEMP/host-$arm.symbols"
          done
          grep -F 'llvm::SelectionDAGBuilder::visitRelocate(' "$RUNNER_TEMP/host-repaired.symbols"
          # Positive control for the producer's uncoloured-symbol assertion.
          nm --defined-only "$CJCJ_BOOTSTRAP_COLOUR_LLVM_SO" > "$RUNNER_TEMP/colour-control.nm"
          c++filt < "$RUNNER_TEMP/colour-control.nm" > "$RUNNER_TEMP/colour-control.symbols"
          awk 'index($0, "llvm::isCJTypedReadHelperCandidate(") {print; n++} END {print "HOST_COLOUR_POSITIVE_CONTROL count=" n+0}' "$RUNNER_TEMP/colour-control.symbols"

      - name: Bootstrap stage0 compiler
        shell: bash
        run: bash ci/bootstrap/gha_run.sh stage0

      - name: Add Linux swap for stage2 chir heap
        if: runner.os == 'Linux'
        run: |
          sudo fallocate -l 12G /mnt/swapfile
          sudo chmod 600 /mnt/swapfile
          sudo mkswap /mnt/swapfile
          sudo swapon /mnt/swapfile
          free -h

      - name: Bootstrap stage1 compiler
        shell: bash
        run: bash ci/bootstrap/gha_run.sh stage1

      - name: Inject selfhost compiler version
        env:
          SOURCE_SDK_VERSION: ${{ steps.version.outputs.value }}
        run: |
          npx --yes zx@8 ci/srcbuild/tests/inject-version.test.mjs
          npx --yes zx@8 ci/srcbuild/steps/inject-version.mjs

      - name: Build stage 3 compiler and final std
        env:
          CJCJ_BOOTSTRAP_WORK: ${{ env.CANGJIE_WORKSPACE }}/bootstrap-work
        run: npx --yes zx@8 ci/srcbuild/steps/build-stage3.mjs

      - name: Upload final source-built std install root
        uses: actions/upload-artifact@v7
        with:
          name: final-std-${{ matrix.target }}
          path: ${{ env.CANGJIE_WORKSPACE }}/software/final-std-stage2
          if-no-files-found: error
          retention-days: 7

      - name: Build stdx from source
        env:
          CJCJ_SRCBUILD_CONSUMER_SDK: ${{ env.CANGJIE_WORKSPACE }}/software/cangjie
        run: npx --yes zx@8 build/cli.mjs --target "${{ matrix.target }}" --build-type "${{ inputs.build_type || 'relwithdebinfo' }}" --cangjie-version "${{ steps.version.outputs.value }}" build stdx

      - name: Build SDK tools from source
        env:
          CJCJ_SRCBUILD_CONSUMER_SDK: ${{ env.CANGJIE_WORKSPACE }}/software/cangjie
        run: npx --yes zx@8 build/cli.mjs --target "${{ matrix.target }}" --build-type "${{ inputs.build_type || 'relwithdebinfo' }}" --cangjie-version "${{ steps.version.outputs.value }}" build tools

      - name: Prepare source-built cjpm artifact
        run: |
          node ci/release/prepare_cjpm_artifact.mjs \
            --platform "${{ matrix.target }}" \
            --binary "$CANGJIE_WORKSPACE/cangjie_tools/cjpm/dist/cjpm" \
            --outdir "$GITHUB_WORKSPACE/.release-cjpm/${{ matrix.target }}"

      - name: Upload source-built cjpm artifact
        uses: actions/upload-artifact@v7
        with:
          name: source-cjpm-${{ matrix.target }}
          path: ${{ github.workspace }}/.release-cjpm/${{ matrix.target }}
          if-no-files-found: error
          retention-days: 7

      - name: Prepare source-built hle artifact
        run: |
          node ci/release/prepare_hle_artifact.mjs \
            --platform "${{ matrix.target }}" \
            --binary "$CANGJIE_WORKSPACE/cangjie_tools/hyperlangExtension/target/bin/main" \
            --outdir "$GITHUB_WORKSPACE/.release-hle/${{ matrix.target }}"

      - name: Upload source-built hle artifact
        uses: actions/upload-artifact@v7
        with:
          name: source-hle-${{ matrix.target }}
          path: ${{ github.workspace }}/.release-hle/${{ matrix.target }}
          if-no-files-found: error
          retention-days: 7

      - name: Package and verify SDK
        env:
          CJCJ_SRCBUILD_CONSUMER_SDK: ${{ env.CANGJIE_WORKSPACE }}/software/cangjie
        run: |
          npx --yes zx@8 build/cli.mjs --target "${{ matrix.target }}" --build-type "${{ inputs.build_type || 'relwithdebinfo' }}" --cangjie-version "${{ steps.version.outputs.value }}" package
          npx --yes zx@8 build/cli.mjs --target "${{ matrix.target }}" --build-type "${{ inputs.build_type || 'relwithdebinfo' }}" --cangjie-version "${{ steps.version.outputs.value }}" verify

      - name: Build compiler shim
        env:
          CANGJIE_CPP_SRC: ${{ env.CANGJIE_WORKSPACE }}/cangjie_compiler
        run: npx --yes zx@8 ci/srcbuild/steps/build-shim.mjs

      - name: Restore official MinGW toolchain
        if: matrix.target == 'linux-x64'
        id: windows-mingw-cache
        uses: actions/cache/restore@v6
        with:
          path: ${{ env.CANGJIE_BUILD_ROOT }}/llvm-mingw-w64
          key: windows-final-std-mingw-20220906-5c68a1cb123161b54b72ce90e7975d95a8eaf2a4-${{ hashFiles('build/toolchain/mingw.mjs', 'build/lib/archive.mjs', 'build/lib/runner.mjs') }}

      - name: Install Windows cross toolchain dependencies
        if: matrix.target == 'linux-x64' && steps.windows-mingw-cache.outputs.cache-hit != 'true'
        run: npx --yes zx@8 build/cli.mjs --target windows-x64 --build-root "$CANGJIE_BUILD_ROOT" install-system-deps

      - name: Build official MinGW toolchain
        if: matrix.target == 'linux-x64' && steps.windows-mingw-cache.outputs.cache-hit != 'true'
        run: npx --yes zx@8 build/cli.mjs --target windows-x64 --build-root "$CANGJIE_BUILD_ROOT" install-mingw

      - name: Save official MinGW toolchain
        if: matrix.target == 'linux-x64' && steps.windows-mingw-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v6
        with:
          path: ${{ env.CANGJIE_BUILD_ROOT }}/llvm-mingw-w64
          key: ${{ steps.windows-mingw-cache.outputs.cache-primary-key }}

      - name: Cross-build Windows final std with stage2 host compiler
        if: matrix.target == 'linux-x64'
        env:
          CJCJ_STAGE3_CANGJIE_VERSION: ${{ steps.version.outputs.value }}
        run: npx --yes zx@8 ci/srcbuild/steps/build-windows-final-std.mjs

      - name: Upload Windows source-built std install root
        if: matrix.target == 'linux-x64'
        uses: actions/upload-artifact@v7
        with:
          name: final-std-windows-x64
          path: ${{ env.CANGJIE_WORKSPACE }}/software/final-std-windows-stage2
          if-no-files-found: error
          retention-days: 7

      - name: Upload stage diagnostics
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: srcbuild-diagnosis-${{ matrix.target }}-${{ github.run_attempt }}
          retention-days: 7
          if-no-files-found: warn
          path: |
            ${{ env.CANGJIE_WORKSPACE }}/software/cangjie/bin/cjcj-stage1
            target/release/**/*.cjo

      - name: Compose self-hosted SDK
        env:
          SOURCE_SDK_VERSION: ${{ steps.version.outputs.value }}
        run: npx --yes zx@8 ci/srcbuild/steps/compose-sdk.mjs

      - name: Archive final compiler handoff
        run: tar -C "$CANGJIE_WORKSPACE/software/final-compiler" -czf "$CANGJIE_WORKSPACE/software/final-compiler.tar.gz" .

      - name: Upload final compiler handoff
        uses: actions/upload-artifact@v7
        with:
          name: final-compiler-${{ matrix.target }}
          path: ${{ env.CANGJIE_WORKSPACE }}/software/final-compiler.tar.gz
          if-no-files-found: error
          retention-days: 7

      - name: Verify self-hosted SDK
        run: npx --yes zx@8 ci/srcbuild/steps/verify.mjs "$CANGJIE_WORKSPACE/software/cangjie"

      - name: Package source language tuple
        if: inputs.language_tuple
        shell: bash
        run: |
          set -euo pipefail
          source ci/llvm_pin.env
          python3 ci/release/source_language_tuple.py pack \
            --sdk "$CANGJIE_WORKSPACE/software/cangjie" \
            --compiler "$CANGJIE_WORKSPACE/software/final-compiler" \
            --std "$CANGJIE_WORKSPACE/software/final-std-stage2" \
            --runtime "$CJCJ_BOOTSTRAP_COLOUR_RT" \
            --host "$CJCJ_SRCBUILD_HOST_SDK/runtime/lib/linux_x86_64_cjnative" \
            --host-identity "$CJCJ_TOOLCHAIN" \
            --llvm-manifest "$CJCJ_FIXED_LLVM_DIR/llvm-tools.manifest" \
            --cjcj-sha "$GITHUB_SHA" --runtime-sha "$RUNTIME_REF" --llvm-sha "$LLVM_SHA" \
            --run-id "$GITHUB_RUN_ID" --run-attempt "$GITHUB_RUN_ATTEMPT" \
            --output "$CANGJIE_WORKSPACE/software/source-language-tuple"

      - name: Upload source language tuple
        if: inputs.language_tuple
        uses: actions/upload-artifact@v7
        with:
          name: source-language-tuple-linux-x64
          path: |
            ${{ env.CANGJIE_WORKSPACE }}/software/source-language-tuple/*.tar.gz
            ${{ env.CANGJIE_WORKSPACE }}/software/source-language-tuple/language-tuple.json
            ${{ env.CANGJIE_WORKSPACE }}/software/source-language-tuple/SHA256SUMS
          if-no-files-found: error
          retention-days: 7

      - name: Upload self-hosted SDK and gate logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cjcj-srcbuild-${{ matrix.target }}-${{ steps.version.outputs.value }}
          path: |
            ${{ env.CANGJIE_WORKSPACE }}/software/cangjie-sdk-*-${{ steps.version.outputs.value }}-cjcj.tar.gz
            ${{ env.CANGJIE_WORKSPACE }}/software/cangjie-sdk-*-${{ steps.version.outputs.value }}-cjcj.tar.gz.sha256
            ${{ runner.temp }}/cjcj-srcbuild-verify/*.log
            .srcbuild/shim-build.log
          if-no-files-found: warn

      # Enforced only on a green job: a job that already failed before its first
      # compile would otherwise report a second, misleading error.
      - name: Report sccache
        if: always()
        uses: ./.github/actions/sccache-report
        with:
          component: srcbuild
          platform: ${{ matrix.target }}
          require-compiles: ${{ job.status == 'success' }}

  artifact-contract:
    name: Publish final std artifact contract
    needs: srcbuild
    runs-on: ubuntu-22.04
    outputs:
      final_compiler_linux_x64: ${{ steps.contract.outputs.final_compiler_linux_x64 }}
      final_std_linux_x64: ${{ steps.contract.outputs.final_std_linux_x64 }}
      final_compiler_linux_aarch64: ${{ steps.contract.outputs.final_compiler_linux_aarch64 }}
      final_std_linux_aarch64: ${{ steps.contract.outputs.final_std_linux_aarch64 }}
      final_compiler_darwin_x64: ${{ steps.contract.outputs.final_compiler_darwin_x64 }}
      final_std_darwin_x64: ${{ steps.contract.outputs.final_std_darwin_x64 }}
      final_compiler_darwin_arm64: ${{ steps.contract.outputs.final_compiler_darwin_arm64 }}
      final_std_darwin_arm64: ${{ steps.contract.outputs.final_std_darwin_arm64 }}
      final_std_windows_x64: ${{ steps.contract.outputs.final_std_windows_x64 }}
    steps:
      - name: Export artifact names
        id: contract
        run: |
          {
            echo "final_compiler_linux_x64=final-compiler-linux-x64"
            echo "final_std_linux_x64=final-std-linux-x64"
            echo "final_compiler_linux_aarch64=final-compiler-linux-aarch64"
            echo "final_std_linux_aarch64=final-std-linux-aarch64"
            echo "final_compiler_darwin_x64=final-compiler-darwin-x64"
            echo "final_std_darwin_x64=final-std-darwin-x64"
            echo "final_compiler_darwin_arm64=final-compiler-darwin-arm64"
            echo "final_std_darwin_arm64=final-std-darwin-arm64"
            echo "final_std_windows_x64=final-std-windows-x64"
          } >> "$GITHUB_OUTPUT"
